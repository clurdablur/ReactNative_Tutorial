// import libraries for manking a component
import React from 'react';
import { Text } from 'react-native';

// make a component

const Header = () => {
    return <Text>Albums</Text>;
};


// make the component available to other parts of the app

export default Header;